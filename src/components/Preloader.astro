<f-preloader
  class=":uno: bg-neutral-950 flex gap-10px items-center inset-0 justify-center fixed z-1000"
  transition:persist
>
  {
    Array(6)
      .fill(null)
      .map((_val, idx) => (
        <div
          class=":uno: rounded-sm bg-white h-1 w-1 animate-[f-preloader-stretch_1s_ease-in-out_infinite]"
          style={{ animationDelay: `${idx * 0.1}s` }}
        />
      ))
  }
</f-preloader>

<style lang="scss" is:global>
  @keyframes f-preloader-stretch {
    0% {
      height: 5px;
    }
    50% {
      height: 30px;
    }
    100% {
      height: 5px;
    }
  }
</style>
